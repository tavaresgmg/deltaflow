# CLAUDE.md

Claude Code reads this file; Codex reads `AGENTS.md`. To keep one source of truth, this
imports `AGENTS.md` instead of duplicating it (Principle 5: one source -> both harnesses).

@AGENTS.md
