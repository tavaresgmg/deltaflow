# Plan

1. Reproduce the current parser risk without starting a harness.
2. Add strict option parsing and explicit `--out`, `--label`, and `--model` support.
3. Document safe runner usage.
4. Add local validation for parser failure modes.
5. Run validation, archive, commit, and push.
